export class Breadcrumbs{
    constructor(
        status: string,
        caption: string,
        icon: string,
        label: string,
        url: string,
        push:string
    ) {
        
    }
}